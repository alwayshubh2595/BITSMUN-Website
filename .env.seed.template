# Copy to .env.seed and paste the values from your deploy platform:
#   Netlify : Site configuration -> Environment variables
#   Vercel  : Project Settings -> Environment Variables
#
# .env.seed is gitignored. Delete it when you are done.
# These are PUBLIC values (Vite inlines them into the browser bundle), so the
# risk here is not secrecy — it is that they are unauthenticated write endpoints.

VITE_SANITY_PROJECT_ID="h5reu7l4"
VITE_DELREGFORM_URL=""
VITE_EBREGFORM_URL=""
VITE_INTDELREGFORM_URL=""
VITE_COUPONS_URL=""
VITE_INTCOUPONS_URL=""
